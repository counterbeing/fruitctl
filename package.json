{
  "name": "fruitctl",
  "private": true,
  "packageManager": "pnpm@10.30.1",
  "scripts": {
    "build": "nx run-many -t build",
    "test": "nx run-many -t test",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "link-cli": "pnpm build && pnpm --filter @fruitctl/cli run bundle && cd apps/cli && pnpm link --global"
  },
  "devDependencies": {
    "@biomejs/biome": "^2",
    "nx": "^22",
    "typescript": "^5.9"
  }
}
